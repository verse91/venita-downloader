import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>
      <h1>Venita Popup</h1>
      <p>This is the popup for the Venita extension.</p>
    </div>
  </React.StrictMode>,
);
